import React from 'react'

const Title = ({ title, subtitle }) => {
    return (
        <>
            <span>{title}</span><br />
            <span>subtitle</span>
        </>
    )
}

export default Title